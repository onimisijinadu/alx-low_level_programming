#include "main.h"

/**
 * main - void print_alphabet_x10(void);
 *
 * Return: Always 0.
 */

void print_alphabet_x10(void)
{
	char c;
	if (c <=10)
		_putchar(c);
	c++;
	for
		(c = 'a';
		 c <= 'z';
		 c++)
			_putchar(c);
	putchar('\n');
	return (0);
}

