#include <stdio.h>

/**
 * Main - Entry point
 *
 * Return: Always 0 (sucess)
 *
 * using putchar inplace of printf
 */

int main(void)
{
	char c;

	for
		(c = 'a'; c <= 'z'; c++)
			putchar(c);

	return (0);
}
